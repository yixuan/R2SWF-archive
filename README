This is an R package to convert several images to a Flash file using
the Ming library.

To install the 'package' here:

install.packages(c('Rd2roxygen', 'formatR'))
setwd('where/is/R2SWF')
rab('R2SWF', install = TRUE)
